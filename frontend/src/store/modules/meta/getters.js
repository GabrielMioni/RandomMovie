const secureBaseUrl = (state) => state.secureBaseUrl

const baseUrl = (state) => state.baseUrl

const posterSizes = (state) => state.posterSizes

export default {
  baseUrl,
  secureBaseUrl,
  posterSizes
}
