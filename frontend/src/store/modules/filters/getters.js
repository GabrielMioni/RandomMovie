const countries = (state) => state.countries

const decades = (state) => state.decades

const directors = (state) => state.directors

const genres = (state) => state.genres

export default {
  countries,
  decades,
  directors,
  genres
}
